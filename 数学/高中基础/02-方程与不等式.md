# 二、方程与不等式


## 章节导学

这一章先抓住三句话：

- 方程研究的是“哪些数代进去后相等”；
- 不等式研究的是“哪些数代进去后大小关系成立”；
- 真正的难点往往不是方法，而是有没有先看清定义域、分母、根号、对数这些限制条件。

## 2.1 基础方程

这一节到底在学什么：

- 学的是“把未知数解出来”；
- 从一元一次方程，到一元二次方程，再到判别式、韦达定理，都是高中基础核心；
- 后面很多函数题，本质上还是在解方程。

老师这样讲：

- 一元一次方程：移项、去括号、去分母；
- 一元二次方程：能分解先分解，不行再配方或用公式；
- 有参数时，经常要用判别式；
- 已知两根关系时，经常用韦达定理。

示例题：

解方程：$2x^2-7x+3=0$

讲解：

先看能不能因式分解。我们想找两个数，使它们乘积是 $2\times3=6$，和是 $-7$。

可以想到 $-6$ 和 $-1$。

于是拆项：

$$
2x^2-7x+3=2x^2-6x-x+3
$$

分组：

$$
=2x(x-3)-1(x-3)
$$

$$
=(2x-1)(x-3)
$$

所以：

$$
(2x-1)(x-3)=0
$$

解得：

$$
x=\frac12 \quad \text{或} \quad x=3
$$

易错点：

- 先整理成标准式再做；
- 分解不出来再用求根公式，不要死扛；
- 韦达定理不是直接给根，而是给根的和与积。

补充理解：判别式、求根公式、韦达定理

先把三者的作用分清：

- 判别式是先判断“这个一元二次方程有没有实数根、有几个实数根”；
- 求根公式是直接把根算出来；
- 韦达定理不是直接给根，而是告诉你两根的和与积。

设一元二次方程的标准式为：

$$
ax^2+bx+c=0 \quad (a\ne0)
$$

### 1. 判别式

判别式定义为：

$$
\Delta=b^2-4ac
$$

它的作用是判断根的情况：

- 当 $\Delta>0$ 时，方程有两个不相等的实数根；
- 当 $\Delta=0$ 时，方程有两个相等的实数根；
- 当 $\Delta<0$ 时，方程没有实数根。

为什么它能判断根的情况？

因为求根公式里会出现 $\sqrt{b^2-4ac}$。根号里面是正数、零还是负数，直接决定了能不能在实数范围内开方，也就决定了方程的实数根情况。

### 2. 求根公式

求根公式是：

$$
x=\frac{-b\pm\sqrt{b^2-4ac}}{2a}
$$

它的作用是：当方程不能顺利因式分解时，可以直接求出根。

推导过程：

从一般式开始：

$$
ax^2+bx+c=0
$$

两边同时除以 $a$：

$$
x^2+\frac ba x+\frac ca=0
$$

把常数项移到右边：

$$
x^2+\frac ba x=-\frac ca
$$

为了把左边配成完全平方，要补上 $\left(\frac b{2a}\right)^2$：

$$
x^2+\frac ba x+\left(\frac b{2a}\right)^2=-\frac ca+\left(\frac b{2a}\right)^2
$$

左边变成完全平方：

$$
\left(x+\frac b{2a}\right)^2=-\frac ca+\frac{b^2}{4a^2}
$$

右边通分时，把 $-\frac ca$ 化成分母为 $4a^2$ 的分数：

$$
-\frac ca=-\frac ca\cdot\frac{4a}{4a}=-\frac{4ac}{4a^2}
$$

所以：

$$
-\frac ca+\frac{b^2}{4a^2}=-\frac{4ac}{4a^2}+\frac{b^2}{4a^2}=\frac{b^2-4ac}{4a^2}
$$

于是得到：

$$
\left(x+\frac b{2a}\right)^2=\frac{b^2-4ac}{4a^2}
$$

两边开平方：

$$
x+\frac b{2a}=\pm\frac{\sqrt{b^2-4ac}}{2a}
$$

移项后就是：

$$
x=\frac{-b\pm\sqrt{b^2-4ac}}{2a}
$$

### 3. 韦达定理

若方程 $ax^2+bx+c=0$ 的两个根是 $x_1,x_2$，那么：

$$
x_1+x_2=-\frac ba,\quad x_1x_2=\frac ca
$$

它的作用是：

- 已知方程，快速求两根的和与积；
- 已知两根的和与积，反过来构造方程；
- 处理和根有关的整体代数式。

推导过程：

如果一个一元二次方程的两根是 $x_1,x_2$，那么它可以写成：

$$
a(x-x_1)(x-x_2)=0
$$

先展开括号：

$$
a[x^2-(x_1+x_2)x+x_1x_2]=0
$$

再展开：

$$
ax^2-a(x_1+x_2)x+ax_1x_2=0
$$

和原方程

$$
ax^2+bx+c=0
$$

逐项对比系数：

$$
-a(x_1+x_2)=b,\quad ax_1x_2=c
$$

所以：

$$
x_1+x_2=-\frac ba,\quad x_1x_2=\frac ca
$$

一个最常见的例子是：

$$
x^2-5x+6=0
$$

这里 $a=1,b=-5,c=6$，所以：

$$
x_1+x_2=5,\quad x_1x_2=6
$$

这时就能很快想到两根是 $2$ 和 $3$。注意，韦达定理本身给的是“和与积”，不是直接把根写出来。

## 2.2 分式方程与高次方程

这一节到底在学什么：

- 学的是“把看起来复杂的方程变回你认识的样子”；
- 分式方程本质上是去分母；
- 高次方程本质上是降次、拆因式。

分式方程统一流程：

1. 先看分母，写出限制条件；
2. 找最简公分母；
3. 两边同乘公分母；
4. 解完以后一定验根。

示例题：

解分式方程：$\frac{2}{x}=\frac{3}{x+1}$

讲解：

先写限制条件：

$$
x\ne0,\quad x\ne-1
$$

交叉相乘：

$$
2(x+1)=3x
$$

展开：

$$
2x+2=3x
$$

移项得：

$$
x=2
$$

检验：

- $x=2$ 不会让分母变成 0；
- 所以是原方程的解。

最终答案：

$$
x=2
$$

易错点：

- 分式方程不验根很容易出增根；
- 去分母后不要忘记原式的定义域限制；
- 高次方程常见套路是“提公因式”或“换元”。

## 2.3 不等式

这一节到底在学什么：

- 学的是“解出满足条件的范围”；
- 和方程不同，不等式的答案通常是区间，不是单个值；
- 一元二次不等式和绝对值不等式是重点。

老师这样讲：

- 一元一次不等式和方程差不多，只是乘除负数要变号；
- 一元二次不等式先找根，再看开口方向；
- 绝对值不等式看到“小于”想到夹在中间，看到“大于”想到两边分开。

示例题：

解不等式：$x^2-5x+6>0$

讲解：

先因式分解：

$$
x^2-5x+6=(x-2)(x-3)
$$

于是原不等式变成：

$$
(x-2)(x-3)>0
$$

要让两个因式乘积大于 0，就要同号。

- 当 $x<2$ 时，两个因式都负，乘积大于 0；
- 当 $2<x<3$ 时，一正一负，乘积小于 0；
- 当 $x>3$ 时，两个因式都正，乘积大于 0。

所以解集是：

$$
(-\infty,2)\cup(3,+\infty)
$$

易错点：

- 不等式答案要写区间；
- 大于 0 和大于等于 0 不一样，端点是否取到要分清；
- 一元二次不等式不能只靠“感觉”，最好画数轴。

